<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AnyQuestion" parent="Theme.AppCompat.DayNight.NoActionBar"></style>

    <style name="Theme.AnyQuestion.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.AnyQuestion.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.AnyQuestion.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>